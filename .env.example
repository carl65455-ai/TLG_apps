# LAN IP of the machine running the app (used by appctl.sh for CORS and API base)
LAN_IP=192.168.1.245

# Ports (change if in use)
API_PORT=4200
WEB_PORT=5180

# Converter
# Set to 0 if you only want GLB/GLTF support
USE_CONVERTER=1
